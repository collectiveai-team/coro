"""ML Model Integrations — external ASR and diarization backends.

Each sub-module adapts one external library and converts its native types
into Project-Owned Transcript Model types at adapter edges.
"""
