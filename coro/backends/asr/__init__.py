"""ASR Backend Providers — transcript-token ML Model Integrations.

Each module adapts one external ASR library into an ASR Adapter that
produces Project-Owned Transcript Model tokens. The ASR Backend Adapter
Factory in ``factory.py`` builds the configured adapter at startup.
"""
